# Security policy

Only the latest `main` commit is supported during public alpha. Report
vulnerabilities privately through GitHub Security Advisories, including
affected versions, reproduction steps, and impact.

GPU AI Hub is for trusted users on a private network. Do not expose the hub or
provider ports to the public internet. Provider ports bind to loopback unless
the installer is explicitly told to expose them to the LAN.
